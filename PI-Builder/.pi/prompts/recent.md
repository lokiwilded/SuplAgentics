---
description: Show the last few SuplAgentics (or a given project's) sessions.
argument-hint: "[project] [count]"
---
Call `supl_sessions` with project="${1:-SuplAgentics}" and limit=${2:-8}. Show the result as a compact numbered list (time — title — session id). Then ask if I want to review any of them (`supl_session`) or start on one.
